#include "Complex.h"

public


Complex readComplex(char* msg) {
	Complex c;
	printf("%s", msg);
	scanf("%lf#lf", &c.real, &c.imag);
	return c;
}

void printComplex(Complex c, char* msg) {
	printf("%")
}